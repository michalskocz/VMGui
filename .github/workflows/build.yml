name: Build VMGui to Fat JAR

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Make gradlew executable
        if: runner.os == 'Linux'
        run: chmod +x ./gradlew

      - name: Build shadowJar (Linux)
        if: runner.os == 'Linux'
        run: ./gradlew clean shadowJar

      - name: Build shadowJar (Windows)
        if: runner.os == 'Windows'
        run: .\gradlew.bat clean shadowJar

      - name: Pack artifact (Linux)
        if: runner.os == 'Linux'
        run: |
          cd build/libs
          jar_name=$(ls *.jar | head -n 1)
          zip "VMGui-${{ runner.os }}.zip" "$jar_name"

      - name: Pack artifact (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          $jar = Get-ChildItem build/libs/*.jar | Select-Object -First 1
          Compress-Archive -Path $jar.FullName -DestinationPath "build/libs/VMGui-${{ runner.os }}.zip" -Force

      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: VMGui-${{ runner.os }}
          path: build/libs/VMGui-${{ runner.os }}.zip
          if-no-files-found: error